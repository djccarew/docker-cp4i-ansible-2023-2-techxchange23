apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ cp4i_ace.subscription_name }}
  namespace: {{ cp4i_ace.subscription_namespace }}
  labels:
    operators.coreos.com/ibm-appconnect.openshift-operators: ''
spec:
  channel: {{ cp4i_ace.subscription_channel }}
  installPlanApproval: Automatic
  name: ibm-appconnect
  source: appconnect-operator-catalogsource
  sourceNamespace: openshift-marketplace
